name: 'telecom_cx_analytics'
version: '1.0.0'
config-version: 2

profile: 'telecom_cx_analytics'

model-paths: ["models"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  telecom_cx_analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
